<h1> Make changes to details  </h1>
<%= form_for :leader,url: leaders_path do |f| %>
<% if @leader.errors.any? %>
   <div id="error_explanation">
     <h2>
       <%= pluralize(@leader.errors.count, "error") %> prohibited
       this article from being saved:
     </h2>
     <ul>
       <% @leader.errors.full_messages.each do |msg| %>
         <li><%= msg %></li>
       <% end %>
     </ul>
   </div>
 <% end %>
  <p>
    <%= f.label :Name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', leaders_path %>
